# Development-specific dependencies
# Use this file for tools not required in production
# Install with: pip install -r requirements-dev.txt

black
ruff
isort
pre-commit

# Testing & coverage
pytest==8.4.1
coverage==7.10.2

# Windows-only helper for libmagic (dev convenience)
python-magic-bin==0.4.14; platform_system == "Windows"
